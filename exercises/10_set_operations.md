# SQL Set Operations

### 🟢 Basic

1. List all first names from both the `actor` and `customer` tables (remove duplicates).
2. List all first names from both the `actor` and `customer` tables, including duplicates.
3. Find city names that appear in both the `address` and `city` tables.
4. Get a combined list of film titles and customer first names.
5. List distinct store IDs from both `inventory` and `staff`.

### 🟡 Intermediate

1. List customer last names who have made payments or rented movies.
2. Find film IDs that are in `inventory` but not in `rental`.
3. Find actor IDs who acted in films and customer IDs who rented films.
4. Find staff IDs that appear in both `payment` and `rental`.
5. List film titles that are not present in the `inventory`.

### 🟠 Advanced

1. List language IDs used in the `film` table and those present in the `language` table.
2. Find actors who acted in films but never in any ‘Horror’ films.
3. Get email addresses of customers who either rented or made a payment.
4. List all film IDs that are in either `inventory` or `rental`.
5. Find films that were rented but are no longer in inventory.

### 🔴 Applied Scenarios

1. Find customer IDs who rented but never made a payment.
2. Find customer IDs who both rented and made a payment.
3. Get film IDs rented by customers who never paid anything.
4. List film IDs that are in inventory more than once and have been rented.
5. Find customer IDs who paid but have no rentals.

### ⚫ Mixed Logic

1. Get a list of first names from both staff and customers.
2. Find store IDs associated with staff but not inventory.
3. List all languages used in films and that exist in the `language` table.
4. List film titles that have never been rented.
5. Find actor IDs who acted only in films that were never rented.